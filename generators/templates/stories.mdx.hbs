import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { {{pascalCase name}} } from '.';

<Meta
  title='Components/{{pascalCase name}}'
  component={ {{pascalCase name}} }
  args={ {} }
  argTypes={ {} }
  parameters={ {} }
/>

export const Template = ({ ...args }) => <{{pascalCase name}} {...args} />;

# {{pascalCase name}}

<!-- Adicionar uma descrição para o componente -->

O componente `{{pascalCase name}}` é ...

## Import

```sh
  import { {{pascalCase name}} } from '@dsdemo/components';
```

## Basic

<Canvas>
  <Story
    name="Default"
    args={ {} }
    argTypes={ {} }
    parameters={ {} }
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={ {{pascalCase name}} } />
